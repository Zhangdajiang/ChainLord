# 📦 环境安装指导

## 第一步：安装 Python

确保你的电脑已安装 Python 3.12 或更高版本。

检查 Python 版本：
```bash
python --version
```

如果没有安装 Python，请访问 https://www.python.org/ 下载安装。

## 第二步：安装依赖包

在项目目录下，运行以下命令安装所有必需的依赖：

### 方法一：使用 pip 逐个安装

```bash
# 安装 Streamlit（网页框架）
pip install streamlit

# 安装 SpoonOS SDK
pip install spoon-ai-sdk

# 安装环境变量管理工具
pip install python-dotenv

# 安装 OpenAI（如果使用 OpenAI）
pip install openai

# 安装异步支持库
pip install nest-asyncio
```

### 方法二：使用 requirements.txt 一次性安装（推荐）

```bash
pip install -r requirements.txt
```

## 第三步：配置 API Key

1. **创建 .env 文件**：
   - 复制 `.env.example` 文件，重命名为 `.env`
   - 在 Windows 上：`copy .env.example .env`
   - 在 Mac/Linux 上：`cp .env.example .env`

2. **编辑 .env 文件**，填入你的 API Key：
   ```
   # 至少需要配置一个 LLM API Key
   OPENAI_API_KEY=sk-your-openai-key-here
   
   # 或者使用 Anthropic Claude
   ANTHROPIC_API_KEY=sk-ant-your-claude-key-here
   
   # 或者使用 DeepSeek
   DEEPSEEK_API_KEY=your-deepseek-key-here
   ```

3. **获取 API Key**：
   - OpenAI: https://platform.openai.com/api-keys
   - Anthropic: https://console.anthropic.com/
   - DeepSeek: https://platform.deepseek.com/

## 第四步：运行应用

在项目目录下运行：

```bash
streamlit run app.py
```

应用会自动在浏览器中打开，默认地址是 `http://localhost:8501`

## 常见问题

### Q: 提示 "请先安装 spoon-ai-sdk"
A: 运行 `pip install spoon-ai-sdk` 安装 SDK

### Q: 提示 "请在 .env 文件中配置至少一个 LLM API Key"
A: 检查 `.env` 文件是否存在，并且至少配置了一个 API Key

### Q: 钱包连接功能不工作
A: 当前版本使用演示模式，实际钱包连接需要配置 SpoonOS SDK 的完整功能

### Q: AI 回复出错
A: 检查你的 API Key 是否有效，以及账户是否有足够的余额

## 需要帮助？

- 查看 README.md 了解详细使用说明
- 查看 SpoonOS SDK 文档：https://github.com/XSpoonAi/spoon-core
- 查看 Streamlit 文档：https://docs.streamlit.io/
